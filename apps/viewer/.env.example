# Optional. Full URL to trees.pmtiles when hosted somewhere that supports HTTP Range (206), e.g. jsDelivr or object storage.
# When unset, the app uses same-origin /trees.pmtiles (local dev and builds without this var).
# VITE_PMTILES_URL=https://cdn.jsdelivr.net/gh/matteopolak/ottawa-trees@main/apps/viewer/public/trees.pmtiles
